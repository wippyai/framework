version: "1.0"
namespace: app

entries:
  - name: user
    kind: registry.entry
    meta:
      comment: Policy group for user relay hubs under test

  - name: processes
    kind: process.host
    lifecycle:
      auto_start: true
