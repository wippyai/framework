version: "1.0"
namespace: wippy.bootloader.env

entries:
  # wippy.bootloader.env:encryption_key
  - name: encryption_key
    kind: env.variable
    meta:
      comment: Auto-generated hex-encoded encryption key for data security
      icon: tabler:lock
      auto_generated: true
      private: true
    readonly: false
    storage: # Will be set by app_env_storage requirement
    variable: ENCRYPTION_KEY