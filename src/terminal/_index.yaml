version: "1.0"
namespace: wippy.terminal

entries:
  - name: definition
    kind: ns.definition
    meta:
      title: Terminal
      description: Terminal UI components and utilities

  # wippy.terminal:host
  - name: host
    kind: terminal.host
    meta:
      comment: Default terminal host for CLI processes
    hide_logs: false
    lifecycle:
      auto_start: true

